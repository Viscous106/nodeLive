# nodeLive backend — Python 3.12, FastAPI stack
# Pin to compatible-release ranges; lock exact versions in CI.

# Web framework + ASGI
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.20

# Database (async)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Real-time
python-socketio==5.12.1

# Auth
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4

# Background jobs
celery==5.4.0
redis==5.2.1

# AI
anthropic==0.42.0

# Config / validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Observability + safety
structlog==24.4.0
slowapi==0.1.9
sentry-sdk[fastapi]==2.19.2

# Storage / http
aiofiles==24.1.0
boto3==1.35.92
httpx==0.28.1

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.6
