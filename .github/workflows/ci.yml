name: CI

on:
  # Run on every branch push so devs get CI feedback before opening a PR,
  # and on PRs to main (which test the merge result).
  push:
    branches: ['**']
  pull_request:
    branches: [main]

# Cancel superseded runs on feature branches to save minutes, but never on
# main — every commit on main must keep its own full, green run (back-to-back
# pushes would otherwise leave the earlier commit showing a cancelled ✗).
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  backend:
    name: Backend (ruff + pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: edustream
          POSTGRES_USER: edustream
          POSTGRES_PASSWORD: localdev
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U edustream"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql+asyncpg://edustream:localdev@localhost:5432/edustream
      DIRECT_DATABASE_URL: postgresql+asyncpg://edustream:localdev@localhost:5432/edustream
      TEST_DATABASE_URL: postgresql+asyncpg://edustream:localdev@localhost:5432/linkhq_test
      REDIS_URL: redis://localhost:6379/0
      AUTH_SECRET: ci-test-secret

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Lint (ruff)
        run: ruff check .

      - name: Format check (ruff)
        run: ruff format --check .

      - name: Apply migrations
        run: alembic upgrade head

      # No manual test-DB step: tests/conftest.py creates linkhq_test if missing,
      # so CI runs exactly what a dev runs locally.
      - name: Test (pytest)
        run: pytest

  frontend:
    name: Frontend (typecheck + build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build (tsc -b && vite build)
        run: npm run build
